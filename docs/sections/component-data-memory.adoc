=== Data Memory

The Data Memory is a sequential component used to store and retrieve data values during load and store instructions. In this simplified processor, the data memory contains eight 8-bit memory locations.

==== Specifications

image::./images/data_mem.png[Data Memory Circuit Diagram, pdfwidth=100%, align=center]

The Data Memory component has the following input and output signals:

[cols="1,1,3", options="header"]
.Data Memory Signals
|===
| Signal
| Bitwidth
| Description

| `clk_in`
| 1
| Clock input. Memory writes and reset behavior occur synchronously when the clock is triggered.

| `reset`
| 1
| Synchronous reset signal. When enabled and the clock is triggered, the memory returns to its initialized values.

| `mem_write`
| 1
| Memory write enable signal. When `mem_write = 1`, the value on `wd` is stored at the selected address on a clock pulse.

| `mem_read`
| 1
| Memory read enable signal. When `mem_read = 1`, the selected memory value appears at the output `rd`.

| `addr`
| 8
| Memory address input used to select the memory location.

| `wd`
| 8
| Write data input. This value is written into memory when `mem_write = 1`.

| `rd`
| 8
| Read data output.
|===

The memory read output depends on `mem_read`. When `mem_read = 1`, the memory outputs the selected value. When `mem_read = 0`, the output is disabled and returns zero. Writes occur only when `mem_write = 1` and a clock pulse is applied.

==== Testing Methodology

The Data Memory was tested inside CircuitVerse by manually applying address, write data, read enable, write enable, reset, and clock values.

The tests were designed to verify the following behavior:

. The memory responds correctly to reset.
. The memory outputs initialized values when read.
. A memory location can be written when `mem_write = 1`.
. The written memory value can be read back correctly.
. A memory location is not modified when `mem_write = 0`.
. The read output is disabled when `mem_read = 0`.
. The memory returns to its initialized values after reset.

The complete test cases are documented in `tests/Data Memory Tests.csv`.

==== Test Cases and Results

[cols="1,3,3,1", options="header"]
.Data Memory Test Cases
|===
| Test
| Inputs Applied
| Expected Output
| Result

| DM_RESET_READ_M0
| `clk_in=1`, `reset=1`, `mem_write=0`, `mem_read=1`, `addr=00000000`, `wd=00000000`
| `rd=01010000`
| Passed

| DM_READ_M1
| `clk_in=0`, `reset=0`, `mem_write=0`, `mem_read=1`, `addr=00000001`, `wd=00000000`
| `rd=01010001`
| Passed

| DM_READ_M7
| `clk_in=0`, `reset=0`, `mem_write=0`, `mem_read=1`, `addr=00000111`, `wd=00000000`
| `rd=01010111`
| Passed

| DM_WRITE_M2_CC
| `clk_in=1`, `reset=0`, `mem_write=1`, `mem_read=1`, `addr=00000010`, `wd=11001100`
| `rd=11001100`
| Passed

| DM_BLOCK_WRITE_M2
| `clk_in=1`, `reset=0`, `mem_write=0`, `mem_read=1`, `addr=00000010`, `wd=11111111`
| `rd=11001100`
| Passed

| DM_READ_DISABLED
| `clk_in=0`, `reset=0`, `mem_write=0`, `mem_read=0`, `addr=00000010`, `wd=00000000`
| `rd=00000000`
| Passed

| DM_RESET_AFTER_WRITES_M2
| `clk_in=1`, `reset=1`, `mem_write=0`, `mem_read=1`, `addr=00000010`, `wd=00000000`
| `rd=01010010`
| Passed
|===

==== Evidence

The following images show the internal CircuitVerse tests used to validate the Data Memory behavior.

image::./images/data_mem_reset.png[Data Memory Reset and Initial Read Test, pdfwidth=100%, align=center]

image::./images/data_mem_write_read.png[Data Memory Write and Read Test, pdfwidth=100%, align=center]

image::./images/data_mem_write_disabled.png[Data Memory Write Disabled Test, pdfwidth=100%, align=center]

image::./images/data_mem_read_disabled.png[Data Memory Read Disabled Test, pdfwidth=100%, align=center]

image::./images/data_mem_reset_after_writes.png[Data Memory Reset After Writes Test, pdfwidth=100%, align=center]

==== Verification

All test cases produced the expected data memory outputs. The reset test confirmed that the memory returns to its initialized values when `reset` is asserted and a clock pulse is applied. The read tests confirmed that the output `rd` correctly reflects the value stored at the selected address when `mem_read = 1`.

The write test verified that memory address `00000010` is updated to `11001100` only when `mem_write = 1` and the clock is triggered. The disabled-write test confirmed that memory contents remain unchanged when `mem_write = 0`, since address `00000010` remained equal to `11001100` even when `wd = 11111111`. The disabled-read test confirmed that the output is cleared to `00000000` when `mem_read = 0`. Finally, the reset-after-writes test confirmed that memory address `00000010` returns to its initialized value `01010010` after reset. The Data Memory component is validated for integration into the processor datapath.